{
  "id": "unique-paths",
  "title": "Unique Paths",
  "difficulty": "Medium",
  "tags": ["math", "dynamic-programming", "matrix"],
  "prompt": "There is a robot on an `m x n` grid. The robot is initially located at the top-left corner (i.e., `grid[0][0]`). The robot tries to move to the bottom-right corner (i.e., `grid[m - 1][n - 1]`). The robot can only move either down or right at any point in time.\n\nGiven the two integers `m` and `n`, return the number of possible unique paths that the robot can take to reach the bottom-right corner.",
  "examples": [
    {"input": "m = 3, n = 7", "output": "28\nExplanation: From the top-left corner, there are a total of 28 unique paths to reach the bottom-right corner."},
    {"input": "m = 3, n = 2", "output": "3\nExplanation: There are three paths: Right->Down->Down, Down->Down->Right, Down->Right->Down."}
  ],
  "constraints": [
    "1 <= m, n <= 100"
  ],
  "starterCode": {
    "python": "def solve(m, n):\n    pass",
    "javascript": "function solve(m, n) {\n  \n}",
    "typescript": "export function solve(m: number, n: number): number {\n  \n}",
    "java": "class Solution {\n    public int solve(int m, int n) {\n        \n    }\n}",
    "csharp": "public class Solution {\n    public int Solve(int m, int n) {\n        \n    }\n}",
    "go": "func solve(m int, n int) int {\n    \n}"
  },
  "visibleTests": [
    {"stdin": {"python": "print(solve(3, 7))"}, "expectedStdout": "28"},
    {"stdin": {"python": "print(solve(3, 2))"}, "expectedStdout": "3"}
  ],
  "hiddenTests": [
    {"stdin": {"python": "print(solve(1, 1))"}, "expectedStdout": "1"},
    {"stdin": {"python": "print(solve(7, 3))"}, "expectedStdout": "28"},
    {"stdin": {"python": "print(solve(10, 10))"}, "expectedStdout": "48620"}
  ],
  "notesForAI": {
    "coreIdea": "DP: dp[i][j] = dp[i-1][j] + dp[i][j-1]. Base case: first row and first column are all 1. Alternatively use combinatorics: C(m+n-2, m-1).",
    "commonBugs": ["Off-by-one in grid dimensions vs number of moves", "Not initializing the first row/column to 1", "Integer overflow with large grids when using combinatorics in some languages"],
    "expectedComplexity": "O(m*n) DP or O(min(m,n)) combinatorics"
  }
}
