{
  "id": "regular-expression-matching",
  "title": "Regular Expression Matching",
  "difficulty": "Hard",
  "tags": ["strings", "dynamic-programming"],
  "prompt": "Given an input string `s` and a pattern `p`, implement regular expression matching with support for `.` and `*` where:\n\n- `.` matches any single character.\n- `*` matches zero or more of the preceding element.\n\nThe matching should cover the entire input string (not partial).\n\nReturn `True` if the pattern matches the entire string, `False` otherwise.",
  "examples": [
    {"input": "s = \"aa\", p = \"a\"", "output": "False\nExplanation: 'a' does not match the entire string 'aa'."},
    {"input": "s = \"aa\", p = \"a*\"", "output": "True\nExplanation: '*' means zero or more of the preceding element, 'a'. Therefore 'a*' matches 'aa'."},
    {"input": "s = \"ab\", p = \".*\"", "output": "True\nExplanation: '.*' means zero or more of any character, matching 'ab'."}
  ],
  "constraints": [
    "1 <= s.length <= 20",
    "1 <= p.length <= 20",
    "s contains only lowercase English letters.",
    "p contains only lowercase English letters, '.', and '*'.",
    "It is guaranteed for each appearance of '*', there will be a previous valid character to match."
  ],
  "starterCode": {
    "python": "def solve(s: str, p: str) -> bool:\n    pass",
    "javascript": "function solve(s, p) {\n  \n}",
    "typescript": "export function solve(s: string, p: string): boolean {\n  \n}",
    "java": "class Solution {\n    public boolean solve(String s, String p) {\n        \n    }\n}",
    "csharp": "public class Solution {\n    public bool Solve(string s, string p) {\n        \n    }\n}",
    "go": "func solve(s string, p string) bool {\n    \n}"
  },
  "visibleTests": [
    {"stdin": {"python": "print(solve(\"aa\", \"a\"))"}, "expectedStdout": "False"},
    {"stdin": {"python": "print(solve(\"aa\", \"a*\"))"}, "expectedStdout": "True"}
  ],
  "hiddenTests": [
    {"stdin": {"python": "print(solve(\"ab\", \".*\"))"}, "expectedStdout": "True"},
    {"stdin": {"python": "print(solve(\"aab\", \"c*a*b\"))"}, "expectedStdout": "True"},
    {"stdin": {"python": "print(solve(\"mississippi\", \"mis*is*ip*.\"))"}, "expectedStdout": "True"}
  ],
  "notesForAI": {
    "coreIdea": "Use 2D DP where dp[i][j] = whether s[0..i-1] matches p[0..j-1]. Base case: dp[0][0] = True. Handle '*' by either taking zero occurrences (dp[i][j-2]) or one+ occurrences (dp[i-1][j] if s[i-1] matches p[j-2]). Handle '.' as matching any character.",
    "commonBugs": ["Not handling the base case where pattern like 'a*b*c*' can match empty string", "Confusing '*' as matching any character (it modifies the preceding element)", "Off-by-one errors in the DP table indexing between 0-indexed strings and 1-indexed DP"],
    "expectedComplexity": "O(m * n) where m = len(s), n = len(p)"
  }
}
